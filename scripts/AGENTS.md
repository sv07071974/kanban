# Scripts agent notes

This folder contains start and stop scripts for macOS, Linux, and Windows.
Each script builds the Docker image and runs (or stops) the single-container
app on port 8000. Start scripts remove any existing container with the same
name before running.